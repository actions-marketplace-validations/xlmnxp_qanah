name: Test Qanah Action

on:
  workflow_dispatch:

jobs:
  peer1:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: ./
        with:
          config: |
            [Interface]
            PrivateKey = gHENMX9KrI5VujkWQaHPPLzCT5x2PPx9hDozVzRUhV8=
            Address = 10.0.0.1/24, fd00::1/64

            [Peer]
            PublicKey = T2b30sEqbgi+tS+MxgPszMVAP9n62yYwTrpLKRiKP1w=
            AllowedIPs = 10.0.0.2/32, fd00::2/128
          ping: 10.0.0.2
          ping-timeout: '120'

  peer2:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: ./
        with:
          config: |
            [Interface]
            PrivateKey = WM6LYgsGyTD9KNvvpo6rZPB9lE+RB/tH6reB+iMpyHc=
            Address = 10.0.0.2/24, fd00::2/64

            [Peer]
            PublicKey = E8aWx/CO+VuEfG+mqp0RsdK+sWK7c3apFFKOQ/zjOzI=
            AllowedIPs = 10.0.0.1/32, fd00::1/128
          ping: 10.0.0.1
          ping-timeout: '120'
